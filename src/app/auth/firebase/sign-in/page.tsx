import FirebaseSignInView from 'src/sections/auth/firebase/firebase-sign-in-view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Firebase: SignIn',
};

export default function SignInPage() {
  return <FirebaseSignInView />;
}
