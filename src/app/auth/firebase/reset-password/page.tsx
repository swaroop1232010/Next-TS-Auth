import { FirebaseResetPasswordView } from 'src/sections/auth/firebase';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Firebase: Reset Password',
};

export default function ResetPasswordPage() {
  return <FirebaseResetPasswordView />;
}
